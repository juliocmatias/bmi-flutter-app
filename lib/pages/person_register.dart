import 'package:flutter/material.dart';

class PersonRegister extends StatelessWidget {
  const PersonRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
